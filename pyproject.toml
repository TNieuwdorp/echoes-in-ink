[project]
name = "echoes-in-ink"
version = "0.1.0"
description = "A Claude Code output style derived from the Polars blog"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
# Scripts declare their own dependencies with PEP 723 inline metadata.
# Run them with: uv run scripts/<name>.py
